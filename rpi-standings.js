(()=>{'use strict';
const root=document.getElementById('featureRoot');if(!root)return;
const classes=['6A','5A','4A','3A','2A','1A','8P'];
const bracketFormats={
  '6A':{cutoff:16,opening:'sixteen'},
  '5A':{cutoff:16,opening:'sixteen'},
  '4A':{cutoff:16,opening:'sixteen'},
  '3A':{cutoff:13,opening:'thirteen'},
  '2A':{cutoff:9,opening:'nine'},
  '1A':{cutoff:9,opening:'nine'},
  '8P':{cutoff:11,opening:'eleven'}
};
const h=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
const norm=value=>String(value??'').trim().toUpperCase().replace(/\s+/g,' ');
const value=value=>Number.isFinite(Number(value))?Number(value).toFixed(6):'—';
const get=async(file,fallback)=>{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);try{const response=await fetch(`${file}?v=${Date.now()}`,{cache:'no-store',signal:controller.signal});return response.ok?await response.json():fallback}catch{return fallback}finally{clearTimeout(timer)}};
async function run(){
  const [data,teams]=await Promise.all([get('uhsaa-rpi-official-2026.json',null),get('teams-data.json',[])]);
  if(!data?.classifications)throw new Error('RPI data unavailable');
  const meta=new Map(teams.map(team=>[norm(team.team),team]));
  const pill=team=>{const item=meta.get(norm(team))||{};return `<a class="team-pill" style="--bg:${h(item.backgroundColor||'#222')};--fg:${h(item.textColor||'#fff')}" href="team.html?team=${encodeURIComponent(team)}">${h(team)}</a>`};
  const bracketSlot=(rows,slot)=>{if(slot===null)return '<span class="bracket-slot-seed"></span><span class="bracket-bye">BYE</span>';if(typeof slot==='string')return `<span class="bracket-slot-seed"></span><span class="bracket-placeholder">${h(slot)}</span>`;const row=rows.find(item=>item.rank===slot);return row?`<span class="bracket-slot-seed">${slot}</span>${pill(row.team)}`:`<span class="bracket-slot-seed">${slot}</span><span class="bracket-placeholder">TBD</span>`};
  const bracketRounds=classification=>{const type=bracketFormats[classification].opening;if(type==='sixteen')return [['First Round',[[1,16],[8,9],[4,13],[5,12],[2,15],[7,10],[3,14],[6,11]]],['Quarterfinals',[['Winner 1/16','Winner 8/9'],['Winner 4/13','Winner 5/12'],['Winner 2/15','Winner 7/10'],['Winner 3/14','Winner 6/11']]],['Semifinals',[['Winner QF1','Winner QF2'],['Winner QF3','Winner QF4']]],['Championship',[['Winner SF1','Winner SF2']]]];if(type==='thirteen')return [['First Round',[[1,null],[8,9],[4,13],[5,12],[2,null],[7,10],[3,null],[6,11]]],['Quarterfinals',[[1,'Winner 8/9'],['Winner 4/13','Winner 5/12'],[2,'Winner 7/10'],[3,'Winner 6/11']]],['Semifinals',[['Winner QF1','Winner QF2'],['Winner QF3','Winner QF4']]],['Championship',[['Winner SF1','Winner SF2']]]];if(type==='nine')return [['Play-In',[[8,9]]],['Quarterfinals',[[1,'Winner 8/9'],[4,5],[2,7],[3,6]]],['Semifinals',[['Winner QF1','Winner QF2'],['Winner QF3','Winner QF4']]],['Championship',[['Winner SF1','Winner SF2']]]];return [['Play-In',[[8,9],[7,10],[6,11]]],['Quarterfinals',[[1,'Winner 8/9'],[4,5],[2,'Winner 7/10'],[3,'Winner 6/11']]],['Semifinals',[['Winner QF1','Winner QF2'],['Winner QF3','Winner QF4']]],['Championship',[['Winner SF1','Winner SF2']]]]};
  const bracket=(classification,rows)=>{const format=bracketFormats[classification],rounds=bracketRounds(classification);return `<section class="projected-bracket"><div class="projected-bracket-head"><div><h3>Projected Playoff Bracket</h3><p>Full bracket path using the current official UHSAA RPI seeds.</p></div><span>Top ${format.cutoff}</span></div><div class="bracket-scroll"><div class="bracket-track">${rounds.map(([label,games],roundIndex)=>`<section class="bracket-stage"><h4>${label}</h4><div class="bracket-games">${games.map(([first,second])=>`<div class="bracket-game${roundIndex===rounds.length-1?' bracket-final':''}"><div class="bracket-slot">${bracketSlot(rows,first)}</div><div class="bracket-slot">${bracketSlot(rows,second)}</div></div>`).join('')}</div></section>`).join('')}<section class="bracket-stage bracket-champion"><h4>Champion</h4><div class="bracket-games"><div class="champion-box"><span>🏆</span><strong>TBD</strong></div></div></section></div></div></section>`};
  const updated=data.fetchedAt?new Date(data.fetchedAt).toLocaleString():'';
  const tabs=classes.map((classification,index)=>`<button class="rpi-tab${index===0?' active':''}" type="button" data-rpi-class="${classification}">${classification==='8P'?'8-Player':classification}</button>`).join('');
  const sections=classes.map((classification,index)=>{const rows=data.classifications[classification]?.rows||[],cutoff=bracketFormats[classification].cutoff;return `<section class="rpi-class" data-rpi-section="${classification}"${index?' hidden':''}><h2 class="section-title">${classification==='8P'?'8-Player':classification} RPI</h2><div class="table-wrap"><table><thead><tr><th>RPI Rank</th><th class="left">Team</th><th>Record</th><th>RPI Value</th><th>MWP</th><th>OWP</th><th>OOWP</th></tr></thead><tbody>${rows.map(row=>`<tr class="${row.rank<=cutoff?'playoff-team':'outside-playoffs'}"><td class="rank">${row.rank}</td><td class="left">${pill(row.team)}</td><td><strong>${h(row.record)}</strong></td><td><strong>${value(row.rpi)}</strong></td><td>${value(row.mwp)}</td><td>${value(row.owp)}</td><td>${value(row.oowp)}</td></tr>${row.rank===cutoff?`<tr class="playoff-cutoff-row"><td colspan="7"><span>PLAYOFF CUTOFF</span></td></tr>`:''}`).join('')}</tbody></table></div>${bracket(classification,rows)}</section>`}).join('');
  root.innerHTML=`<div class="feature-note"><strong>Official UHSAA standings.</strong> Every rank, record, RPI, MWP, OWP and OOWP value below comes directly from UHSAA, not the Rural Utah Sports projection. <a href="https://uhsaa.org/football-rpi/" target="_blank" rel="noopener">View the UHSAA source</a>${updated?`<br><strong>Official data synced:</strong> ${h(updated)}`:''}</div><div class="rpi-tabs" role="tablist" aria-label="Football classification">${tabs}</div>${sections}`;
  root.querySelectorAll('[data-rpi-class]').forEach(button=>button.addEventListener('click',()=>{const selected=button.dataset.rpiClass;root.querySelectorAll('[data-rpi-class]').forEach(item=>item.classList.toggle('active',item===button));root.querySelectorAll('[data-rpi-section]').forEach(section=>{section.hidden=section.dataset.rpiSection!==selected})}));
}
run().catch(error=>{console.error(error);root.innerHTML='<div class="empty">The RPI standings could not load. Try refreshing in a moment.</div>'});
})();
