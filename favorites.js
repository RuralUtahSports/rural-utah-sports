(()=>{'use strict';const KEY='rus-favorite-teams-v1',norm=v=>String(v??'').trim().toUpperCase().replace(/\s+/g,' ');function load(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}function save(a){try{localStorage.setItem(KEY,JSON.stringify([...new Set(a.map(norm))].slice(0,5)));window.dispatchEvent(new CustomEvent('rus-favorites-changed'))}catch{}}window.RUSFavorites={load,save,has:t=>load().includes(norm(t)),toggle(t){const n=norm(t),a=load();if(a.includes(n))save(a.filter(x=>x!==n));else if(a.length<5)save([...a,n]);else return false;return true}};if((location.pathname.split('/').pop()||'').toLowerCase()!=='team.html')return;const team=new URLSearchParams(location.search).get('team');if(!team)return;const style=document.createElement('style');style.textContent='.rus-favorite-team{margin-top:14px;background:#171717;color:#fff;border:1px solid #555;border-radius:6px;padding:9px 12px;font-weight:900;text-transform:uppercase;cursor:pointer}.rus-favorite-team.on{border-color:#F14D07;color:#F14D07}';document.head.appendChild(style);function add(){const hero=document.querySelector('#page .hero-content');if(!hero||document.getElementById('rusFavoriteTeam'))return;const b=document.createElement('button');b.id='rusFavoriteTeam';b.className='rus-favorite-team';const paint=()=>{const on=window.RUSFavorites.has(team);b.classList.toggle('on',on);b.textContent=on?'★ Saved to My Teams':'☆ Add to My Teams'};b.onclick=()=>{if(window.RUSFavorites.toggle(team)===false){alert('You can save up to 5 teams. Remove one from My Teams first.');return}paint()};hero.appendChild(b);paint()}add();const page=document.getElementById('page');if(page&&!document.getElementById('rusFavoriteTeam')){const o=new MutationObserver(()=>{add();if(document.getElementById('rusFavoriteTeam'))o.disconnect()});o.observe(page,{childList:true,subtree:true})}})();