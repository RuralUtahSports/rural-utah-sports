(()=>{const ORDER=['totalOffenseYards','passingYards','rushingYards','passingTouchdowns','rushingTouchdowns','sacks','interceptions'];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const slug=s=>String(s||'').trim().toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');async function run(){const team=new URLSearchParams(location.search).get('team')||'';if(!team)return;try{const r=await fetch(`team-stat-single-game-records/by-team/${slug(team)}.json?v=1`,{cache:'no-store'});if(!r.ok)return;const d=await r.json(),host=document.createElement('section');host.id='teamStatRecords';host.style.marginTop='28px';let html=`<h2 class="section-title">Team Records</h2><div class="info-box"><h3>Single-Game Statistical Records</h3><p class="note">${esc(d.coverageNote)}</p>`;for(const key of ORDER){const rows=d.records?.[key];if(!rows?.length)continue;const m=d.categories[key];html+=`<h3 style="margin-top:20px">${esc(m.label)}</h3><div class="table-wrap"><table><thead><tr><th>#</th><th>${esc(m.label)}</th><th>Opponent</th><th>Date</th><th>Score</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${x.rank}</td><td><strong>${esc(x.value)}</strong> ${esc(m.unit)}</td><td>${esc(x.opponent)}</td><td>${esc(x.date)}</td><td>${x.teamScore==null?'':esc(x.teamScore)+'-'+esc(x.opponentScore)}</td></tr>`).join('')}</tbody></table></div>`}html+='</div>';host.innerHTML=html;(document.querySelector('#page')||document.querySelector('main')||document.body).append(host)}catch(e){console.warn('Team statistical records unavailable',e)}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',run,{once:true}):run()})();
