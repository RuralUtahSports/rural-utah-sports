(()=>{const S=window.RUSSeasonSim=window.RUSSeasonSim||{};S.setup=()=>{const c=document.getElementById('seasonContent');if(!c)return;if(typeof simulator==='undefined'||!simulator?.teams||!Object.keys(simulator.teams).length){setTimeout(S.setup,100);return}const teams=Object.keys(simulator.teams).filter(k=>/^(6A|5A|4A|3A|2A|1A|8P|8-PLAYER)$/i.test(String(simulator.teams[k]?.classification||''))).sort(),map=S.teamMap();c.className='';c.innerHTML=`<div class="weekly-controls"><div class="field"><label>Team</label><select id="seasonTeam">${teams.map(t=>`<option>${S.h(t)}</option>`).join('')}</select></div><div class="field"><label>Schedule Season</label><select id="seasonYear"><option>Loading...</option></select></div><button class="action" id="seasonRun" disabled>Simulate Season</button><div id="seasonMsg" class="note"></div></div><div id="seasonOut" class="card prob-card"><strong>Season Sandbox:</strong> choose a team and one of its real regular-season schedules. Historical playoff games are removed, then a new playoff path is generated from the simulated season.</div>`;const ts=document.getElementById('seasonTeam'),ys=document.getElementById('seasonYear'),btn=document.getElementById('seasonRun'),msg=document.getElementById('seasonMsg'),out=document.getElementById('seasonOut'),q=new URLSearchParams(location.search);ts.value=simulator.teams[q.get('seasonTeam')]?q.get('seasonTeam'):(simulator.teams.MANTI?'MANTI':teams[0]);const load=async()=>{btn.disabled=true;msg.textContent='Loading schedule...';try{const d=await S.loadPage(ts.value),years=Object.keys(d.schedules||{}).filter(y=>d.schedules[y]?.length).sort((a,b)=>+b-+a);ys.innerHTML=years.map(y=>`<option>${S.h(y)}</option>`).join('');if(years.includes(q.get('seasonYear')))ys.value=q.get('seasonYear');msg.textContent='';btn.disabled=false}catch(e){msg.textContent='No schedule data found.'}};ts.onchange=load;btn.onclick=async()=>{btn.disabled=true;msg.textContent='Simulating season and playoffs...';try{const d=await S.loadPage(ts.value),raw=d.schedules?.[ys.value]||[],games=S.regularSchedule?await S.regularSchedule(ts.value,ys.value,raw,map):S.cleanSchedule(raw,map),seed=Date.now()%100000,r=S.run(ts.value,games,map,seed),rw=r.out.filter(x=>x.sim.won).length,rl=r.out.length-rw,pct=r.out.length?rw/r.out.length:.5;let p=null,info=null;try{const br=await fetch(`brackets-${ys.value}.json?v=${Date.now()}`).then(x=>x.ok?x.json():null);if(br){info=S.playoffField?.(br,ts.value,map,r.elo,pct);if(info)p=S.playoffPath?.(ts.value,info,r,map,seed)}}catch(e){}const pw=p?.wins||0,pl=p?.losses||0,finalElo=Number(p?.elo??r.elo),start=Number(simulator.teams[ts.value]?.elo)||1500,margin=r.out.length?r.out.reduce((n,x)=>n+x.sim.a-x.sim.b,0)/r.out.length:0,finish=p?.finish||(info&&!info.user?'Missed Playoffs':'Playoff data unavailable');out.className='';out.innerHTML=`<div class="weekly-summary"><div class="mini"><div class="mini-value">${rw}-${rl}</div><div class="mini-label">Regular Season</div></div><div class="mini"><div class="mini-value">${rw+pw}-${rl+pl}</div><div class="mini-label">Overall Record</div></div><div class="mini"><div class="mini-value">${S.h(finish)}</div><div class="mini-label">Playoff Finish</div></div><div class="mini"><div class="mini-value">${p?.seed?'#'+p.seed:'—'}</div><div class="mini-label">Simulated Seed</div></div><div class="mini"><div class="mini-value">${Math.round(finalElo)}</div><div class="mini-label">Final ELO</div></div><div class="mini"><div class="mini-value">${finalElo-start>=0?'+':''}${Math.round(finalElo-start)}</div><div class="mini-label">ELO Change</div></div></div><h3>Regular Season</h3><div class="table-wrap"><table><thead><tr><th>Date</th><th>Opponent</th><th>Win Odds</th><th>Score</th><th>Result</th><th>ELO</th></tr></thead><tbody>${r.out.map(x=>`<tr><td>${S.h(x.game.date)}</td><td class="left stat-team">${S.h(x.sim.opponent)}</td><td>${(x.sim.prob*100).toFixed(1)}%</td><td>${x.sim.a}-${x.sim.b}</td><td class="${x.sim.won?'result-w':'result-l'}">${x.sim.won?'W':'L'}</td><td>${x.change>=0?'+':''}${x.change} → ${Math.round(x.elo)}</td></tr>`).join('')}</tbody></table></div>${p?.qualified?`<h3 style="margin-top:24px">Simulated Playoffs • ${S.h(info?.cls||'')}</h3><div class="table-wrap"><table><thead><tr><th>Round</th><th>Opponent</th><th>Win Odds</th><th>Score</th><th>Result</th><th>ELO</th></tr></thead><tbody>${p.games.map(x=>x.bye?`<tr><td>${S.h(x.round)}</td><td class="left stat-team">BYE</td><td>—</td><td>—</td><td>Advance</td><td>—</td></tr>`:`<tr><td>${S.h(x.round)}</td><td class="left stat-team">${S.h(x.sim.opponent)}</td><td>${(x.sim.prob*100).toFixed(1)}%</td><td>${x.sim.a}-${x.sim.b}</td><td class="${x.sim.won?'result-w':'result-l'}">${x.sim.won?'W':'L'}</td><td>${x.change>=0?'+':''}${x.change} → ${Math.round(x.elo)}</td></tr>`).join('')}</tbody></table></div>`:`<div class="card prob-card" style="margin-top:20px"><strong>${S.h(finish)}</strong><p class="note">No simulated playoff games were added.</p></div>`}<p class="note">Regular-season playoff games are removed using the archived bracket. The new playoff seed and path use the selected year's classification field plus the simulated record and ELO. Other bracket games advance by rating, so this is a what-if simulation rather than an official UHSAA seeding projection. Regular-season average margin: ${margin>=0?'+':''}${margin.toFixed(1)}.</p>`}catch(e){out.innerHTML='<div class="note">Could not complete this season simulation.</div>'}finally{btn.disabled=false;msg.textContent=''}};load()};})();